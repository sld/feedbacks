$("#feedback-form").html("<%= escape_javascript( render :partial => 'feedback_form', :object => @feedback ) %>");

$("#feedback-form").dialog({
  modal: true,
  width: 300,
  height: 550,
  buttons:  {
    "Отправить": function(){
      $("#feedback-form").children().submit();
    },
    "Отмена": function() { $( this ).dialog( "close" ); }
  },
  close:  function() { }
});
